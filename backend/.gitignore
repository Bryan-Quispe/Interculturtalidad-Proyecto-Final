node_modules/
dist/
.env
.env.local
*.log
# Con barra inicial: solo backend/uploads, la carpeta de subidas en tiempo de
# ejecución. Sin ella el patrón coincide con cualquier directorio llamado
# "uploads" a cualquier profundidad, y excluyó el código de
# src/modules/uploads del repositorio.
/uploads/
.DS_Store
